# Backend: параллельная реализация и интеграция — 23 сентября 2026

- Три агента реализовали каталог/список HR, HTTP initial/append импорт и completion.
- Применена миграция 0002_backend_workflows с сохранением существующих данных.
- Полный backend-прогон после параллельной AI-интеграции: 147 passed, включая PostgreSQL.
- Дополнительно добавлена регрессия на дублирующиеся HTTP-маршруты: каталог 9 passed.
- Конкурентные append/completion, повтор импорта после изменения навыков, защита
  от двойного gain и откат транзакций проверены во временных PostgreSQL-схемах.
- Frontend: TypeScript, production build и 8 тестов клиента/прокси прошли.
  Прокси передаёт Idempotency-Key и ограничивает upload 42 MiB.
- Локальное демо пересобрано: DB/API/web healthy. Через localhost:3000 проверены
  каталог, HR-список, повтор initial HTTP-импорта (unchanged), доступ employee/HR
  и повторное завершение уже completed участия без изменения навыков.
- При параллельной сборке найден конфликт обработчиков /events; окончательная
  версия использует один catalog router, отдельный тест предотвращает регрессию.
- Завершение будущей сессии явно обозначено как демо-симуляция. Append принимает
  только новые профили и их историю; обновление существующих профилей не поддерживается.
- Браузерная визуальная проверка не выполнялась: подключённого браузера нет.

# Сквозной демо-вход — 23 сентября 2026

- Локальный Compose пересобран и запущен, все три сервиса healthy.
- Миграция применена, импортированы 200 сотрудников и 2743 записи истории.
- HTTP-проверка через localhost:3000: список профилей, вход сотрудника, HttpOnly cookie,
  auth/me, профиль/навыки/история, отказ чужому ID, logout, неверный пароль HR,
  успешный вход HR и чтение другого профиля — успешно.
- TypeScript и production-сборка в Docker прошли; git diff --check без ошибок.
- UI дополнен двумя кнопками роли и подсказками сотрудников; после входа переход в кабинет.
- Подключённого браузера нет: клики и визуальная проверка не выполнялись.
- Траектория, рекомендации, completion и HR-аналитика по-прежнему ожидают backend.

# Backend: демо-вход/JWT — 23 сентября 2026

- 50 тестов прошли, включая PostgreSQL во временной схеме.
- Проверены вход employee/HR, произвольный новый employee ID, пустая история,
  соответствие навыков/истории БД, пагинация и права на трёх маршрутах чтения.
- Проверены испорченные/просроченные JWT, обязательные claims, issuer/audience,
  алгоритм, отсутствующий субъект, выключенный демо-режим и ошибки настройки.
- Подтверждено отсутствие секретов в ошибках валидации и деталей SQL в 503.
- PostgreSQL: миграции, конкурентный импорт и вход → чтение → отказ чужому профилю.
- `docker compose config --quiet` и `git diff --check` прошли.
- Работающая схема приложения и frontend этим этапом не изменялись.
  Существующий frontend-контракт требует адаптации: [backend-auth](backend-auth.md).
- Предупреждения зависимостей: Starlette/AnyIO и Alembic path_separator.

# Frontend — 23 сентября 2026

- Реализованы UI задач 1.1–1.12, demo-вход, HttpOnly-сессия и business web-прокси.
- `npm run typecheck` — успешно.
- `npm run build -- --webpack` — успешная production-сборка `/`, `/hr`, `/api/v1/[...path]`.
- `npm test` — 4 интеграционных теста прокси и 4 проверки клиентского адаптера
  на фиксированных ответах (не на настоящем FastAPI):
  allowlist/Origin, отсутствие сессии, cookie и передача Bearer, ответы 403,
  completion и его повтор, новый список рекомендаций, multipart-импорт,
  ошибки валидации, недоступность upstream.
- Тесты нашли и помогли исправить ошибку сравнения Origin с внутренним адресом Next.js.
- `git diff --check` — без ошибок.
- Turbopack в этом окружении падает на `binding to a port: Operation not permitted`,
  включая повтор после разрешения. Webpack собирает тот же код успешно.
- Браузеры в инструментах сессии отсутствуют: визуальная проверка desktop/mobile
  и клики по UI не выполнены. Адаптивная вёрстка предусмотрена, но не подтверждена
  визуальным smoke-тестом.
- Фронтенд адаптирован к появившимся параллельно реальным контрактам входа, профиля,
  навыков и истории (включая пагинацию). Остальные бизнес-endpoint'ы ещё отсутствуют.
  Сквозной сценарий с БД,
  серверные права, транзакции и расчёты этими frontend-тестами не проверяются.
  Фикстуры — только тестовые ответы, не production fallback.
- Контракт, запуск fixture API и оставшиеся зависимости: [frontend-api.md](frontend-api.md).

Ниже — исторические проверки предыдущих этапов.

# Проверка каркаса — 23 сентября 2026

## Объединение backend и AI

- Общий набор API/AI: **69 passed, 1 skipped**; PostgreSQL-тест пропущен,
  поскольку `TEST_DATABASE_URL` не задан.
- Telegram-бот: **12 tests OK**.
- Web: TypeScript и production-сборка Next.js прошли.
- Docker Compose: конфигурация валидна. Проверки выполнены после объединения веток.

## Автономный AI-слой

- Pytest: **55 passed** на Python 3.10.3, включая исходные 7 тестов.
- Все 200 профилей датасета проверены на `ru`, `kk`, `en`: вычисления,
  допустимые кандидаты, стабильный порядок и локальные объяснения.
- Проверены адаптация истории и пересчёт после completion, профиль с новым ID,
  повреждённый датасет и ошибочные ответы провайдера.
- OpenAI HTTP-контракт проверен через `httpx.MockTransport`: успех, ошибки,
  таймаут, отказ, неверные IDs/факты, сохранение порядка и fallback.
- CLI `python -m app.ai_demo --employee E0028 --locale ru`: три рекомендации,
  исходная readiness 74.14%, объяснения из шаблонов.
- `docker compose config --quiet` и `git diff --check`: успешно.
- Настоящий запрос OpenAI **не выполнялся**: локальный ключ отсутствует.
  Полная интеграция с БД/JWT/HTTP/UI также ещё не выполнена.
- Сохранилось одно предупреждение deprecation от Starlette/AnyIO.

Команды запуска и live-проверки: [ai-integration.md](ai-integration.md).

## Backend: данные — 23 сентября 2026

- 22 теста прошли, включая PostgreSQL 17 в отдельной временной схеме.
- Реальный набор: 60 навыков, 200 сотрудников, 40 событий, 2743 участия.
- Проверены валидация, FK, восстановление навыков после даты оценки, повторный импорт
  без gain, отказ от перезаписи другого набора и откат всей транзакции при ошибке.
- PostgreSQL: upgrade → downgrade → upgrade, отсутствие расхождений ORM/миграции
  через `alembic check`, два одновременных импорта с результатами imported/unchanged.
- Основная схема работающего приложения не изменялась. JWT, бизнес-API,
  добавочный импорт, completion и аналитика ещё не реализованы.
- Предупреждения зависимостей: Starlette/AnyIO и Alembic `path_separator`.

## Переход на мобильный React web

Native-клиент удалён; сотрудник использует /, HR — /hr в apps/web.
Добавлено пробное задание с локальным прогрессом и доступ к web по Wi-Fi.
После миграции успешно выполнены `npm run typecheck`, `npm run build`
из `apps/web` и `docker compose config --quiet`. Сборка содержит маршруты `/`,
`/hr` и `/api/v1/[endpoint]`. Проверка на физическом телефоне не выполнялась.
Текущие проверки выполнены на Node 20.20.2; npm предупреждает, что версия ниже
заявленной в engines. Для разработки используйте Node 22.13+ по README.
Проверки ниже относятся к исходному каркасу до этой миграции.

## Пройдено до миграции

- Pytest: 7 тестов, включая инварианты роста навыков и health/readiness.
- TypeScript обоих клиентов; production-сборка Next.js.
- Expo SDK 57: проверка совместимости зависимостей, экспорт iOS и Android bundles.
- Docker build API и web, Compose: все три сервиса healthy.
- API health, API readiness, readiness через Next.js: HTTP 200.
- HR-страница: HTTP 200; доступ к readiness с хоста: HTTP 200.
- Alembic current: подключение к PostgreSQL успешно, revisions пока нет.

Проверки JavaScript выполнены на Node 22.23.2, локальные Python-тесты — на Python 3.11,
контейнер API — на Python 3.12. PostgreSQL опубликован на localhost:55432,
поскольку Docker сообщал о конфликтах портов 5432 и 5433.

## Не проверено и известные ограничения

- Визуальная проверка страницы: подключённый браузер недоступен.
- Mobile на физическом устройстве и в запущенном симуляторе не проверен.
  Bundles собраны, но это не заменяет проверку сети и UI на устройстве.
- npm audit web: 0 уязвимостей. Для mobile: 10 moderate-сообщений в цепочке
  Expo tooling → xcode → uuid. Предлагаемый npm автоматический fix откатывает Expo
  на SDK 46; он не применён, чтобы сохранить совместимость SDK 57. Проверить обновление
  upstream перед публикацией приложения.
- Pytest выдаёт одно предупреждение deprecation внутри Starlette/AnyIO;
  тесты проходят.
