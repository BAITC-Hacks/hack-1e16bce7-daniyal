# Этапы реализации

## 1. Каркас — выполнен

- React + Next.js: мобильная страница сотрудника / и HR-экран /hr с проверкой API/БД.
- Первоначальное пробное задание заменено интерфейсом с подключением к API.
- FastAPI, конфигурация, PostgreSQL/SQLAlchemy, Alembic; бизнес-таблицы добавлены на этапе 2.
- Доменные модели, расчёт навыков, раздельные порты scoring и объяснений.
- Роли employee/hr, Pytest, Docker Compose, lock-файлы и инструкции.
- Автономный AI-слой: gaps/readiness, отбор и scoring, OpenAI-план объяснения,
  шаблоны RU/KK/EN, демо на датасете и тесты. См. [интеграцию AI](ai-integration.md).

## 2. Данные и demo-доступ — текущий этап

ТЗ и датасет изучены. Реализованы SQLAlchemy-модели, миграция и валидируемый
CLI-импорт исходных данных с учётом истории после последней оценки.
Read-only адаптер для автономной проверки AI также готов.
[Правила данных](backend-data.md). Реализованы демо-вход/JWT, разграничение employee/hr
и API профиля, навыков, истории: [контракты](backend-auth.md).
Осталось: HR endpoint импорта и добавление проверочных профилей к загруженному набору.

## Frontend — подготовлен к интеграции

Реализованы экраны 1.1–1.12, demo-вход, сессия, бизнес-прокси, поиск и фильтры.
Расчёты поступают только с API; старое localStorage-демо удалено.
Вход, профиль, навыки и история подключены к текущим контрактам backend.
Остальные секции ожидают API; реальные сквозные проверки UI ещё нужны.
[Контракт и проверки](frontend-api.md).

## 3. Сквозной сценарий сотрудника

Профиль → разрывы до следующего грейда → 1–3 шага → завершение активности → новый прогресс.
Подключить реализованный deterministic scoring к данным backend и HTTP API.
Завершение активности сделать транзакционным и идемпотентным.

## 4. Объяснения и HR

Подключить готовый OpenAI-адаптер RU/KZ/EN с проверкой ответа и fallback, проверить живой вызов с ключом.
HR-срез по сотрудникам и навыкам с проверкой доступа. Интеграционные проверки сценариев.

## 5. Размещение demo

Выбрать Render/Railway/VPS по фактической доступности. Настроить HTTPS, секреты,
миграции, проверку readiness и smoke-тест с телефона. Публикация не входит в каркас.
